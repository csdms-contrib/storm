name: Storm
language: c

register: register_bmi_storm
includes:
  - "#include <storm/bmi_storm.h>"
cflags:
  pkgconfig: storm
libs:
  pkgconfig: storm

build:
  brew:
    formula: csdms/models/storm
    options: "--HEAD"
